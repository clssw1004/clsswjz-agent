# 2026-08-22 工作日志

## clsswjz-agent 设计文档审阅
- 审阅了 `docs/superpowers/specs/2026-08-22-clsswjz-agent-design.md`(独立部署的同步代理服务:NestJS + Vue 3 + SQLite,多用户独立数据目录,复用主端 sync 协议)。
- 产出审阅报告:`docs/superpowers/specs/2026-08-22-clsswjz-agent-design-review.md`。
- 关键结论:
  - 4 个 P0:① 同步冲突处理缺失;② 附件同步通道未定义;③ userId 直接拼路径存在注入风险;④ mainToken 加密方案未定义。
  - 5 个 P1:ConnectionManager 无 eviction(文档与代码不一致);synchronize:true 生产风险;users 表 userId 主键换主端冲突;JWT 24h 过短;lastSyncTime 依赖本地时钟。
  - 建议补充章节:冲突解决、附件同步、密钥管理、连接生命周期、备份恢复、错误重试。
- 项目背景:CLSSWJZ 个人财务管理系统,已有 clsswjz-server(NestJS)+ clsswjz-gui(Flutter 离线优先),agent 为新增 Web 端同步代理。

## v2 复核(对照 server/gui 实际源码)
- 撤回 3 项原 P0:① 冲突处理——主端协议已定义(日志追加 + 按 operatedAt ASC 回放 + UPDATE 无条件覆盖,记录级 LWW);② 附件通道——gui 已实现(文件直传 /api/attachments/upload + 元数据走 LogSync + pull 后按需下载);③ userId 路径注入——userId 为 [a-z0-9] 32 位随机串,无风险。
- 撤回 P1-5:pull 游标 syncTimeStamp 为主端时钟,无本地时钟问题。
- 保留 P0:mainToken 加密——主端 token 为 128 位 opaque 随机串、无过期、auth.guard 仅查表;泄露即永久冒充。
- 新增 P1:① 注册流程缺失(主端有 /api/sync/register,gui 双模式);② 实体清单缺 account_book_users(主端 getMyBookIds/canOperateBook 依赖);③ pull 协议细节缺失(分页/commitId/businessTypes 优先级)。
- 关键协议事实(agent 实现需对齐):push 幂等按 log.id;pull 返回主端 syncTimeStamp 作游标;物化规则=LogRunner(部分字段 update、no-op、sanitize);登录需带 clientType/clientId/clientName。
- 审阅报告已更新为 v2:`docs/superpowers/specs/2026-08-22-clsswjz-agent-design-review.md`。

## 设计文档补充(用户决策落地 + UI 交互设计)
- 用户决策:① clientType/clientId/clientName 映射浏览器信息(clientType='web'、clientId=crypto.randomUUID() 存 localStorage、clientName=UA);② agent 端实体与主端全量对齐;③ 定位 web。
- 设计文档已更新:§2.3 登录流程(clientType 映射 + 可选注册)、§2.6 token 失效语义、§3.1 同步协议(push 幂等/pull 分页+commitId+businessTypes/附件两段式/物化规则=LogRunner)、§3.3 pull 流程、§4.2 实体清单(补 User 镜像 + rel_accountbook_user,含命名区分注记)。
- 新增 §5.5-5.11 UI 交互设计:设计令牌(复用管理台 tokens.css:金主色 #f59e0b、玻璃拟态、支出红/收入绿惯例)、核心用户流程、组件规范、页面四态(加载/数据/空/错误)+叠加态(离线/同步中)、关键页面交互细节(字段记忆、乐观 UI)、微交互动效表、无障碍(WCAG AA/44px/键盘)。
- 管理台令牌来源:clsswjz-server/admin-web/src/styles/tokens.css。

## UI 交互基准切换:管理台 → Flutter 移动端
- 用户指出 UI 交互应向移动端靠齐(而非管理台)。对照 gui 源码确认:Flutter 端是记账 App 惯例——金额用计算器面板弹层(calculator_panel.dart)、类型切换动画(animated_type_toggle)、错落入场(_sectionVisible)、顶栏账本选择器(BookSelector)、底部 Tab 记账|统计|工具|我的、编辑页 autoSave、日期+时间分段。
- 设计文档已调整:§5.2 移动优先+顶栏账本选择器+Tab 对齐;§5.3 路由补 /settings/sync;§5.4 计算器面板交互;§5.5 明确"令牌复用管理台(品牌)、交互对齐 Flutter(惯例)";§5.7 金额组件改计算器;§5.9 首页/表单细节对齐 items_tab 与 modern_item_form;§5.10 补计算器弹出/错落入场动效;§5.11 计算器无障碍+键盘兜底。
- 关键决策:视觉令牌(金色/玻璃)复用管理台保品牌一致,交互范式全部以 gui 为基准。

## 视觉基准切换:管理台 → Flutter(M3 白卡片)+ PC 适配
- 用户进一步要求:视觉也向移动端靠齐,同时考虑 PC 浏览器使用。
- 对照 gui theme_provider.dart 确认 Flutter 视觉:Material 3 + ColorScheme.fromSeed(主题色种子可换)、纯白卡片表面(非玻璃拟态)、明暗/字号(0.85-1.3)/圆角(0/4/8/12)可配置、ThemeSpacing 间距系统。
- 设计文档 §5 已更新:§5.1 技术栈去掉管理台 tokens;§5.5 重写为"M3 白卡片 + 主题色种子 + 可配置"并加"桌面端适配原则"(1200px 居中、多列网格、hover 行操作、侧栏、双列表单、快捷键);§5.7 组件(主色按钮/白卡片/主题色 badge);§5.9 桌面增强(hover 编辑删除、Ctrl+K 记账、Enter 保存);§5.8/5.11 去金色。
- 最终定位:交互+视觉均对齐 Flutter 移动端,管理台仅借鉴明暗切换思路;桌面浏览器做响应式增强。
